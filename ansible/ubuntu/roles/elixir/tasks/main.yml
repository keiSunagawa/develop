- name: nix gc
  become_user: "{{ user }}"
  command: "~/.nix-profile/bin/nix-store --gc"

- name: install from nix
  become_user: "{{ user }}"
  command: "~/.nix-profile/bin/nix-env -i elixir-1.8.2"
