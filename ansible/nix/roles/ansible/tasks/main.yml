- name: nix gc
  become_user: "{{ user }}"
  command: "~/.nix-profile/bin/nix-store --gc"

- name: install from nix
  become_user: "{{ user }}"
  command: "~/.nix-profile/bin/nix-env -i ansible-2.8.4"
